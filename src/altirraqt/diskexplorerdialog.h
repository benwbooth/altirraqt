//	Altirra - Qt port
//	Disk Explorer dialog. Mounts an Atari disk image read-only, lists the
//	root directory (and lets you navigate subdirectories), and extracts
//	selected files to the host filesystem.

#pragma once

class QWidget;

void ATShowDiskExplorerDialog(QWidget *parent);
