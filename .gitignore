# Build artifacts
/build/
/build-appimage/
/build-flatpak/
/dist/
/outputs/
/result
/result-*

# nix-appimage symlink
/altirraqt.AppImage

# Cached download tools (linuxdeploy etc.)
/tools/cache/

# Flatpak host-built ROMs staged before flatpak-builder.
/packaging/flatpak/prebuilt-roms/

# flatpak-builder cache
/.flatpak-builder/

# Editor / OS detritus
*~
*.swp
.DS_Store

# Nix / direnv caches
/.direnv/
/result
/result-*

# Per-user Claude Code settings (allowlist, hook registrations)
/.claude/settings.local.json
